package agragecacaocomposicao;

public class AgragecacaoComposicao {

    public static void main(String[] args) {
        
        // Agregação 
        // Uma determinada classe(todo) depende de várias outras(dependentes) para funcionar corretamente.
        // Os elementos existem sem o todo, o todo não existem sem os elementos.
        // Exemplo: As peças de um carro continuam existindo, mas um carro não existe(funciona) sem todas as suas peças.
        
        // Composição 
        // Classes derivadas(dependentes) não existem se a classe principal(todo) as parte não existem sem o todo.
        // Exemplo: Um item de um pedido não existe se o pedido não existir.
    }
}
